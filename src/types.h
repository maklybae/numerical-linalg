#ifndef TYPES_H
#define TYPES_H

#include <cstddef>

namespace linalg::types {

using Size       = std::size_t;
using Index      = std::size_t;
using Difference = std::ptrdiff_t;

}  // namespace linalg::types

#endif
